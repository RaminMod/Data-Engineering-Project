Just trying to see how it works
Used git config user.name "MyUserName"
Using git config --global user.name "MyUserName"
Using git config user.name and git git config user.mail
Running the above but from the main folder

